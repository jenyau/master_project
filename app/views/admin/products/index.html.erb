<p id="notice"><%= notice %></p>

<h1>products</h1>
<div id="header_img">
  <%= image_tag("img/design55.jpg") %>
</div>
    <div id="menu_header_img">
      <%= image_tag("img/menu_front.jpg") %>
    </div>

    <% if @products.empty? %>
      не указано
    <% else %>
      <% @products.each do |product| %>
    <div class="menu">
        <%= image_tag product.image.url(:menu_img) if product.image? %>
    </div>
    <div class="menu">
        <h4><%= link_to "#{ product.name }", admin_product_path(product) %></h4>
        <%= product.description %>
    </div>
    <% end %>
  <% end %>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render @products %>
  </tbody>
</table>




<br>

<%= link_to 'New product', new_admin_product_path %><br>
<%= link_to 'Back', admin_products_path %>
